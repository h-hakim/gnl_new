# get_next_line

A 42 School project that implements a function to read a file or standard input **one line at a time**.  
Each call to `get_next_line()` returns the next line until the end of file or an error occurs.

---

## 📜 Project Overview

**Prototype**  
```c
char *get_next_line(int fd);
